name: app_user_parking
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Versiones actualizadas y compatibles de Firebase
  firebase_core: ^3.2.0
  firebase_auth: ^5.1.2
  cloud_firestore: ^5.6.12
  firebase_ui_auth: ^2.0.0

  # Otras dependencias actualizadas
  google_maps_flutter: ^2.6.1
  font_awesome_flutter: ^10.7.0
  provider: ^6.1.1
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  location: ^6.0.0
  intl: ^0.20.2 # ¡Esta es la corrección clave!
  http: ^1.2.1

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
